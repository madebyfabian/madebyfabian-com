/**
 * @see https://github.com/Diizzayy/nuxt-graphql-client/issues/298
 * We had to disable the codegen, so this is a temporary replacement.
 */
export type TempDataResponse = any

export type ItemBase = {
	id: string
	name: string
}
