query SinglePostBySlug($slug: ID!) {
	post(id: $slug, idType: SLUG) {
		id
		title
		date
		blocks {
			attributesJSON
			name
			innerBlocks {
				attributesJSON
				name
				innerBlocks {
					attributesJSON
					name
					innerBlocks {
						attributesJSON
						name
						innerBlocks {
							attributesJSON
							name
						}
					}
				}
			}
		}
		date
		slug
		tags {
			edges {
				node {
					id
					name
				}
			}
		}
		author {
			node {
				name
			}
		}
	}
}
